---
export type Props = {
  title?: string;
  heading?: "h1" | "h2";
  id: string;
  reverse?: boolean;
};

const { title, heading, id, reverse = false } = Astro.props;
---

<section id={id} class={`two-col ${reverse ? "reverse" : ""}`}>
  <div class="container">
    <div class="flex-group-col">
      {title && heading === "h1" && <h1>{title}</h1>}
      {title && heading === "h2" && <h2>{title}</h2>}
      <slot name="left" />
    </div>
    <div class="right-side">
      <slot name="right" />
    </div>
  </div>

  <style lang="scss">
    @use "@styles" as *;

    .two-col {
      &.reverse {
        .container {
          flex-direction: row-reverse;
        }
      }
      .container {
        flex-direction: column;
        gap: 4rem;
        .flex-group-col {
          align-items: flex-start;
          justify-content: flex-start;
          h1 {
            font-size: var(--font-size-xxxl);
            line-height: 1.4;
            font-weight: $black;
            color: $brand-primary;
          }
          p {
            font-size: var(--font-size-md);
          }
        }
      }
      @include container-query(tablets) {
        .container {
          flex-direction: row;
        }
      }
    }

    #ai-solutions {
      padding: 0;
      background: $gradient-bg;
      .container {
        max-width: 100%;
        .flex-group-col {
          padding: 4rem 3rem 4rem 160px;
          flex: 0 0 55%;
        }
        .right-image {
          flex: 0 0 45%;
          object-position: 60% center;
        }
      }
    }
    #ai-configure {
      background-color: $light;
    }
  </style>
</section>
